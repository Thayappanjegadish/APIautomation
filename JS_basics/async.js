// It is used to perform asynchronous JS operations 
// await keyword can be declared only inside async function
// await keyword makes the function pause the execution and wait for a resolved promise before it continues.

async function numberone() {
    
    let promise = new promise(function(resolve,reject){
            resolve("I love you")
    })

}